<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Laporan Agen <%= agent.username %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body { background-color: #f8f9fa; }
        .container { margin-top: 50px; }
    </style>
</head>
<body>
    <div class="container">
        <a href="/admin" class="btn btn-secondary mb-3">Kembali ke Dashboard Admin</a>
        <h1 class="mb-4">Laporan Agen: <%= agent.username %></h1>
        <p><strong>Saldo Saat Ini:</strong> Rp<%= agent.balance.toLocaleString('id-ID') %></p>

        <div class="card mb-4">
            <div class="card-header">Riwayat Transaksi</div>
            <div class="card-body">
                <table class="table table-striped">
                    <thead>
                        <tr>
                            <th>Tipe</th>
                            <th>Jumlah</th>
                            <th>Kode Voucher</th>
                            <th>Tanggal</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% if (transactions.length > 0) { %>
                            <% transactions.forEach(t => { %>
                                <tr>
                                    <td><%= t.type %></td>
                                    <td>Rp<%= t.amount.toLocaleString('id-ID') %></td>
                                    <td><%= t.voucher_id || '-' %></td>
                                    <td><%= new Date(t.transaction_date).toLocaleString('id-ID') %></td>
                                </tr>
                            <% }); %>
                        <% } else { %>
                            <tr>
                                <td colspan="4" class="text-center">Belum ada riwayat transaksi untuk agen ini.</td>
                            </tr>
                        <% } %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>